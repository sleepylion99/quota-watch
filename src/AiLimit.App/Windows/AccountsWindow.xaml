<Window x:Class="AiLimit.App.Windows.AccountsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:AiLimit.App.Controls"
        Title="Accounts"
        Icon="/Assets/Icons/app.ico"
        Width="780"
        Height="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Border Margin="8"
            Background="{DynamicResource Brush.Surface.Window}"
            BorderBrush="{DynamicResource Brush.Border.Default}"
            BorderThickness="1"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="46" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Padding="18,0,10,0"
                    Background="{DynamicResource Brush.Surface.TitleBar}"
                    CornerRadius="11,11,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding WindowTitle}"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource Brush.Text.Primary}"
                               FontSize="15"
                               FontWeight="Bold" />
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Button Style="{StaticResource WindowMinimizeButtonStyle}"
                                Margin="0,0,4,0"
                                Content="&#x2212;"
                                Click="MinimizeButton_Click" />
                        <Button Style="{StaticResource WindowCloseButtonStyle}"
                                Content="&#x00D7;"
                                Click="CloseButton_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <TabControl x:Name="AccountsTabsView"
                        Grid.Row="1"
                        Margin="16,10,16,14"
                        Background="Transparent"
                        BorderThickness="0"
                        ItemsSource="{Binding Tabs}"
                        SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="Bd"
                                            Padding="9,7"
                                            Margin="0,0,4,0"
                                            Background="Transparent"
                                            CornerRadius="8">
                                        <ContentPresenter ContentSource="Header" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Surface.CardElevated}" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Accent.Primary}" />
                                            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.OnAccent}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding TabHeader}"
                                   Margin="6,2"
                                   FontSize="12"
                                   FontWeight="SemiBold" />
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <controls:AccountsTable DataContext="{Binding}" />
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>

            <controls:AccountTrashView x:Name="TrashView"
                                       Grid.Row="1"
                                       Margin="16,10,16,14"
                                       Visibility="Collapsed" />
        </Grid>
    </Border>
</Window>
